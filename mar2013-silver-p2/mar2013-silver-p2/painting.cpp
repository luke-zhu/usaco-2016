#include <iostream>
#include <fstream>
#include <tuple>

using namespace std;



//O(n log n), sweep line, set of y intervals at each x

int main() {
	ifstream cin("painting.in");
	// ofstream cout("painting.out");

	int n;
	cin >> n;
	Rect rect[50000];
	for (int i = 0; i < n; ++i) {
	}
}